[
    {
        "query": "query GetForecast($latitude: String!, $longitude:String!) { forecast(latitude: $latitude, longitude: $longitude) { forecast } }"
    },
    {
        "query": "query GetAlerts($state: String!) { alerts(state: $state) { severity description instruction } }"
    }
]