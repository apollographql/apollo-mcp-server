type Query {
  penguin(id: ID!): Penguin
  colonies: [Colony!]!
}

type Penguin {
  id: ID!
  species: String!
  bandedAt: String
}

type Colony {
  id: ID!
  name: String!
  populationEstimate: Int
}
