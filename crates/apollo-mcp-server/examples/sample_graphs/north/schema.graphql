type Query {
  iceberg(id: ID!): Iceberg
  glaciers: [Glacier!]!
}

type Iceberg {
  id: ID!
  name: String!
  massTonnes: Float!
}

type Glacier {
  id: ID!
  name: String!
  retreatMetersPerYear: Float
}
