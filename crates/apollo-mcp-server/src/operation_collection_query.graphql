query OperationCollectionQuery($collectionEntryIds: [ID!]!) {
  operationCollectionEntries(collectionEntryIds: $collectionEntryIds) {
    currentOperationRevision {
      body
      headers {
        name
        value
      }
      variables
    }
    id
    name
    lastUpdatedAt
  }
}